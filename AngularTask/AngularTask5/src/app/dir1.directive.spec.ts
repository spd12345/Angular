import { Dir1Directive } from './dir1.directive';

describe('Dir1Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir1Directive();
    expect(directive).toBeTruthy();
  });
});
